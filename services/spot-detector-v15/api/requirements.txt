fastapi>=0.104.0
uvicorn>=0.24.0
opencv-python-headless>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
Pillow>=10.0.0
python-multipart>=0.0.6
transformers>=4.40.0
ultralytics>=8.2.0
inference-sdk>=0.25.0
# CPU torch/torchvision are installed separately in the Dockerfile (pinned
# to the CPU wheel index) so a default install doesn't pull ~2GB of CUDA
# wheels onto this VM.
