/** Shared with web — keep in sync via re-export. */
export type {
  KaiOnboardingClinical,
  PatientTrackerCause,
  PatientTrackerFocusAction,
  PatientTrackerParamRow,
  PatientTrackerReport,
  PatientTrackerResource,
} from "../../src/lib/patientTrackerReport.types";
