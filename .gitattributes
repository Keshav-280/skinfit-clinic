# YOLO acne detector weights — baked into the acne-detector Docker image.
# Stored via Git LFS so the ECS image build is self-contained (no EFS needed).
models/acne-detector/best.pt filter=lfs diff=lfs merge=lfs -text
